def compute_depth(n):
    print(n)
    digits = []
    number = 0
    for i in range(1, 101):
        if len(digits) == 10:
            return i-1
        number = n*i
        for counter in str(number):
            if counter not in digits:
                digits.append(counter)