def persistence(n):
    print(n)
    if n < 10: return 0
    n_str = str(n)
    counter = 0
    while int(n_str) >= 10:
        mult = 1
        for i in str(n_str):
            mult *= int(i)
        n_str = mult
        counter += 1
    else:
        return counter