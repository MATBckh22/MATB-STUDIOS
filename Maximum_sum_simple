def max_sum(arr,ranges): 
    sums = []
    for i in ranges:
        range_sum = 0
        for s in range(i[0],i[1]+1):
            if i[1]+1 > len(arr):
                continue
            range_sum = range_sum + arr[s]
        sums.append(range_sum)
    return max(sums)