def reverse_alternate(string):
    string = " ".join(string.split())
    if len(string) == 0:
        return string
    words = string.split(" ")
    for i in range(1, len(words), 2):
        words[i] = words[i][::-1]
    return " ".join(words)